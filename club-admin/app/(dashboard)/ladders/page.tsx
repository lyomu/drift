"use client";

import { useCallback, useEffect, useState } from "react";
import { api, ApiError } from "@/lib/api-client";
import { useClub } from "@/lib/club-context";
import { Button, Card, EmptyState, ErrorBanner, Field, Input, PageHeader } from "@/components/ui";
import { StatusBadge } from "@/components/StatusBadge";
import type { LadderAdmin } from "@/lib/types";

export default function LaddersPage() {
  const { clubId, role } = useClub();
  const canManage = role === "OWNER" || role === "ADMIN";
  const [ladders, setLadders] = useState<LadderAdmin[] | null>(null);
  const [selected, setSelected] = useState<LadderAdmin | null>(null);
  const [name, setName] = useState(""); const [range, setRange] = useState("2"); const [showForm, setShowForm] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const load = useCallback(async () => { if (!clubId) return; try { setLadders((await api.get<{ ladders: LadderAdmin[] }>(`/clubs/${clubId}/ladders`)).ladders); } catch (err) { setError(err instanceof ApiError ? err.message : "Ladders could not be loaded."); } }, [clubId]);
  useEffect(() => { void load(); }, [load]);
  async function create(e: React.FormEvent) { e.preventDefault(); if (!clubId) return; try { await api.post(`/clubs/${clubId}/ladders`, { name, challengeRange: Number(range) }); setName(""); setShowForm(false); await load(); } catch (err) { setError(err instanceof ApiError ? err.message : "The ladder could not be created."); } }
  async function open(id: string) { if (!clubId) return; try { const res = await api.get<{ ladder: LadderAdmin }>(`/clubs/${clubId}/ladders/${id}`); setSelected(res.ladder); } catch (err) { setError(err instanceof ApiError ? err.message : "The ladder could not be opened."); } }
  function move(index: number, direction: -1 | 1) { if (!selected?.entries) return; const next = [...selected.entries]; const target = index + direction; if (target < 0 || target >= next.length) return; [next[index], next[target]] = [next[target], next[index]]; setSelected({ ...selected, entries: next.map((entry, i) => ({ ...entry, position: i + 1 })) }); }
  async function saveOrder() { if (!clubId || !selected?.entries) return; try { await api.patch(`/clubs/${clubId}/ladders/${selected.id}/positions`, { entries: selected.entries.map((e) => ({ entryId: e.id, position: e.position })) }); await open(selected.id); } catch (err) { setError(err instanceof ApiError ? err.message : "Positions could not be saved."); } }
  async function archive() { if (!clubId || !selected) return; try { await api.patch(`/clubs/${clubId}/ladders/${selected.id}/archive`); setSelected(null); await load(); } catch (err) { setError(err instanceof ApiError ? err.message : "The ladder could not be archived."); } }
  return <div><PageHeader title="Ladder management" description="Create rolling ladders and adjust positions when an administrative ruling requires it." action={canManage ? <Button onClick={() => setShowForm(!showForm)}>{showForm ? "Cancel" : "Create ladder"}</Button> : undefined} /><ErrorBanner message={error} />{showForm && <Card className="mb-5"><form onSubmit={create} className="flex flex-col gap-3 sm:flex-row sm:items-end"><div className="flex-1"><Field label="Ladder name"><Input required value={name} onChange={(e) => setName(e.target.value)} /></Field></div><div className="w-full sm:w-44"><Field label="Challenge range"><Input type="number" min={1} max={10} value={range} onChange={(e) => setRange(e.target.value)} /></Field></div><Button type="submit">Create</Button></form></Card>}{ladders === null ? <EmptyState message="Loading…" /> : ladders.length === 0 ? <EmptyState message="Create your first ladder" /> : <div className="grid grid-cols-1 gap-5 lg:grid-cols-[minmax(260px,0.7fr)_1.3fr]"><div className="flex flex-col gap-2">{ladders.map((ladder) => <button key={ladder.id} onClick={() => void open(ladder.id)} className="rounded-lg border border-drift-border bg-drift-surface p-4 text-left transition-colors hover:border-drift-primary"><div className="flex justify-between gap-2"><span className="font-semibold text-drift-text-primary">{ladder.name}</span><StatusBadge status={ladder.state} /></div><div className="mt-1 text-sm text-drift-text-secondary">{ladder._count?.entries ?? 0} players · challenge {ladder.challengeRange} rungs</div></button>)}</div><div>{!selected ? <EmptyState message="Select a ladder to manage its positions." /> : <Card><div className="mb-4 flex items-center justify-between"><div><h2 className="font-display text-lg font-bold text-drift-text-primary">{selected.name}</h2><p className="mt-1 text-sm text-drift-text-secondary">Rung 1 is the top position.</p></div>{canManage && <Button variant="ghost" onClick={() => void archive()}>Archive</Button>}</div>{!selected.entries?.length ? <EmptyState message="Members join this ladder from the Drift app." /> : <div className="flex flex-col gap-2">{selected.entries.map((entry, index) => <div key={entry.id} className="flex items-center gap-3 rounded-md border border-drift-border px-3 py-2"><span className="w-8 font-display text-lg font-bold text-drift-primary">{entry.position}</span><div className="flex-1"><div className="font-semibold text-drift-text-primary">{`${entry.user.firstName ?? ""} ${entry.user.lastName ?? ""}`.trim() || "Player"}</div><div className="text-xs text-drift-text-secondary">{entry.wins} wins · {entry.losses} losses</div></div>{canManage && <div className="flex gap-1"><Button variant="ghost" disabled={index === 0} onClick={() => move(index, -1)} aria-label="Move up">↑</Button><Button variant="ghost" disabled={index === selected.entries!.length - 1} onClick={() => move(index, 1)} aria-label="Move down">↓</Button></div>}</div>)}{canManage && <Button onClick={() => void saveOrder()} className="mt-3 self-start">Save positions</Button>}</div>}</Card>}</div></div>}</div>;
}
