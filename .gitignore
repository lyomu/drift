# Dependencies
node_modules/

# Frozen pre-redesign snapshot of mobile/lib — local reference only, delete when redesign is signed off
_legacy-mobile-ui/

# Builds
dist/
.next/
out/
build/
.dart_tool/

# Local env files / secrets
.env
.env.local
.env.*.local

# Machine-local Android config & signing keys (mobile/android/.gitignore
# also covers these; repeated here so the whole tree stays safe)
local.properties
key.properties
*.keystore
*.jks

# Preview build artifacts (large binaries; rebuild with the commands in PROGRESS.md)
*.apk

# OS junk
.DS_Store
Thumbs.db

# Prisma CLI output captured during agent-driven migrations
prisma-*.txt
gen-check*.txt
gen-check*.txt
full-e2e-out.txt
w6-e2e.txt
pa-e2e-out.txt
pa-handles.txt


# QA evidence screenshots
qa-evidence/

# Local dev server logs (runtime artifacts; can capture request/response detail)
*.log
